<Window x:Class="antiGGGravity.Views.General.AutoDimensionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Auto Dims" Height="520" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="55" CornerRadius="12" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Style="{StaticResource PremiumBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource CompanyLogoStyle}"/>
                        <TextBlock Style="{StaticResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Auto Dims" Style="{StaticResource PremiumTitleStyle}"/>
                            <TextBlock Text="Auto-dimension grids, walls, columns, foundations" Style="{StaticResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕"
                            Style="{StaticResource PremiumIconButtonStyle}" Width="30" Height="30"
                            Click="UI_Btn_Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20,15,20,0">
                <StackPanel>
                    <!-- Categories Section -->
                    <Border Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Categories" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <CheckBox x:Name="UI_Chk_Grids" Grid.Row="0" Grid.Column="0" Content="Grids" IsChecked="True" Margin="0,4"/>
                                <CheckBox x:Name="UI_Chk_Walls" Grid.Row="0" Grid.Column="1" Content="Walls" IsChecked="True" Margin="0,4"/>
                                <CheckBox x:Name="UI_Chk_Columns" Grid.Row="1" Grid.Column="0" Content="Columns" IsChecked="True" Margin="0,4"/>
                                <CheckBox x:Name="UI_Chk_Foundations" Grid.Row="1" Grid.Column="1" Content="Foundations" IsChecked="True" Margin="0,4"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Offsets Section -->
                    <Border Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Dimension Offsets (mm)" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,8"/>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Dim Offset Row 1" VerticalAlignment="Center" FontSize="12"/>
                                <TextBox x:Name="UI_Txt_Offset1" Grid.Column="1" Text="1000" Height="28" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                            </Grid>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Dim Offset Row 2" VerticalAlignment="Center" FontSize="12"/>
                                <TextBox x:Name="UI_Txt_Offset2" Grid.Column="1" Text="800" Height="28" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                            </Grid>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Grid Chain Offset" VerticalAlignment="Center" FontSize="12"/>
                                <TextBox x:Name="UI_Txt_ChainOffset" Grid.Column="1" Text="500" Height="28" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Grid Chain Gap" VerticalAlignment="Center" FontSize="12"/>
                                <TextBox x:Name="UI_Txt_ChainGap" Grid.Column="1" Text="800" Height="28" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Tolerances Section -->
                    <Border Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock Text="Tolerances (mm)" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,8"/>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Edge on Grid" VerticalAlignment="Center" FontSize="12"/>
                                <TextBox x:Name="UI_Txt_ZeroTol" Grid.Column="1" Text="5" Height="28" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Intersection" VerticalAlignment="Center" FontSize="12"/>
                                <TextBox x:Name="UI_Txt_IntersectTol" Grid.Column="1" Text="50" Height="28" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#FAFAFA" Padding="20,15" CornerRadius="0,0,12,12" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="UI_Btn_Cancel" Grid.Column="1" Content="Cancel" Width="80" Style="{StaticResource PremiumPrimaryButtonStyle}" Margin="0,0,12,0" Click="UI_Btn_Cancel_Click"/>
                    <Button x:Name="UI_Btn_Run" Grid.Column="2" Content="Dimension" Width="100" Style="{StaticResource PremiumActionButtonStyle}" Click="UI_Btn_Run_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
