<Window x:Class="antiGGGravity.Views.General.ModifyWallConstraintsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modify Wall Constraints" Height="450" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="70" CornerRadius="12" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border Style="{StaticResource PremiumBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource CompanyLogoStyle}"/>
                        <TextBlock Style="{StaticResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Wall Constraints" Style="{StaticResource PremiumTitleStyle}"/>
                            <TextBlock Text="Modify Base &amp; Top Constraints" Style="{StaticResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" 
                            Style="{StaticResource PremiumIconButtonStyle}" Width="30" Height="30"
                            Click="UI_Btn_Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="25">
                <TextBlock Text="Modify Base Constraint" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,6"/>
                <CheckBox x:Name="UI_Check_Base" Content="Modify base level" IsChecked="True" Foreground="#666666" Margin="0,0,0,8"/>
                <ComboBox x:Name="UI_Combo_Base" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0" Margin="0,0,0,25">
                     <!-- Levels populated here -->
                </ComboBox>

                <TextBlock Text="Modify Top Constraint" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,6"/>
                <CheckBox x:Name="UI_Check_Top" Content="Modify Top Level" IsChecked="True" Foreground="#666666" Margin="0,0,0,8"/>
                <ComboBox x:Name="UI_Combo_Top" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0" Margin="0,0,0,15">
                     <!-- Levels populated here -->
                </ComboBox>
                
                <TextBlock x:Name="UI_Selection_Info" Text="Confirm levels and click Modify." FontSize="12" Foreground="#999999" FontStyle="Italic" Margin="2,10,0,0"/>
            </StackPanel>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#FAFAFA" Padding="20,15" CornerRadius="0,0,12,12" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock x:Name="UI_Status_Text" Text="Ready" VerticalAlignment="Center" Foreground="#999999"/>

                    <Button Grid.Column="1" Content="Cancel" Click="UI_Btn_Cancel_Click" Width="80" Margin="0,0,12,0" Style="{StaticResource PremiumPrimaryButtonStyle}"/>
                    <Button Grid.Column="2" Content="Modify" Click="UI_Btn_Run_Click" Width="100" Style="{StaticResource PremiumActionButtonStyle}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
