<Window x:Class="antiGGGravity.Views.General.JoinUnjoinView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Join &amp; Unjoin Advanced" Height="650" Width="850"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" Topmost="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="70" CornerRadius="12" GlassFrameThickness="0" ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Style="{StaticResource PremiumBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource CompanyLogoStyle}"/>
                        <TextBlock Style="{StaticResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Join &amp; Unjoin Advanced" Style="{StaticResource PremiumTitleStyle}"/>
                            <TextBlock Text="Batch process geometry joins" Style="{StaticResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" 
                            Style="{StaticResource PremiumIconButtonStyle}" Width="30" Height="30"
                            Click="UI_Btn_Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- OPTIONS -->
            <Border Grid.Row="1" Margin="25,20,25,0" Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Operation:" Style="{StaticResource PremiumSectionLabelStyle}" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <RadioButton x:Name="UI_Radio_Join" Content="Join" IsChecked="True" VerticalAlignment="Center" Margin="0,0,25,0" Foreground="#333333" FontSize="14" FontWeight="SemiBold"/>
                    <RadioButton x:Name="UI_Radio_Unjoin" Content="Unjoin" VerticalAlignment="Center" Foreground="#333333" FontSize="14" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <!-- DUAL PANEL -->
            <Grid Grid.Row="2" Margin="25,20,25,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT: PRIORITY -->
                <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="#F8F9FA" Padding="15,12" CornerRadius="8,8,0,0" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,0,0,1">
                            <TextBlock Text="✂️ Priority (Cutting Elements)" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0"/>
                        </Border>
                        <ListBox x:Name="UI_List_Left" Grid.Row="1" BorderThickness="0" Background="Transparent" Margin="4,8" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="4,2"/>
                                    <Setter Property="Margin" Value="0"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked, Mode=TwoWay}" Padding="8,6" Foreground="#333333"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Border Grid.Row="2" Padding="10" Background="#FAFAFA" CornerRadius="0,0,8,8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                            <UniformGrid Columns="2">
                                <Button Content="All" Click="UI_Btn_LeftAll_Click" Height="32" Margin="5,0" Style="{StaticResource PremiumSecondaryButtonStyle}"/>
                                <Button Content="None" Click="UI_Btn_LeftNone_Click" Height="32" Margin="5,0" Style="{StaticResource PremiumSecondaryButtonStyle}"/>
                            </UniformGrid>
                        </Border>
                    </Grid>
                </Border>

                <!-- ARROW -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="▶" HorizontalAlignment="Center" FontSize="24" Foreground="#3F51B5" Opacity="0.5"/>
                </StackPanel>

                <!-- RIGHT: SECONDARY -->
                <Border Grid.Column="2" Background="White" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="0">
                     <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="#F8F9FA" Padding="15,12" CornerRadius="8,8,0,0" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,0,0,1">
                            <TextBlock Text="🧱 Secondary (To Be Joined)" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0"/>
                        </Border>
                        <ListBox x:Name="UI_List_Right" Grid.Row="1" BorderThickness="0" Background="Transparent" Margin="4,8" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="4,2"/>
                                    <Setter Property="Margin" Value="0"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked, Mode=TwoWay}" Padding="8,6" Foreground="#333333"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Border Grid.Row="2" Padding="10" Background="#FAFAFA" CornerRadius="0,0,8,8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                            <UniformGrid Columns="2">
                                <Button Content="All" Click="UI_Btn_RightAll_Click" Height="32" Margin="5,0" Style="{StaticResource PremiumSecondaryButtonStyle}"/>
                                <Button Content="None" Click="UI_Btn_RightNone_Click" Height="32" Margin="5,0" Style="{StaticResource PremiumSecondaryButtonStyle}"/>
                            </UniformGrid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <!-- FOOTER -->
            <Border Grid.Row="3" Background="#FAFAFA" Padding="20,15" CornerRadius="0,0,12,12" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock x:Name="UI_Status_Text" Grid.Column="0" Text="Select categories and process." VerticalAlignment="Center" Foreground="#999999"/>

                    <Button x:Name="UI_Btn_Cancel" Grid.Column="1" Content="Cancel" Width="90" Style="{StaticResource PremiumPrimaryButtonStyle}" Margin="0,0,12,0" Click="UI_Btn_Cancel_Click"/>
                    <Button x:Name="UI_Btn_Run" Grid.Column="2" Content="Process" Width="140" Style="{StaticResource PremiumActionButtonStyle}" Click="UI_Btn_Run_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
