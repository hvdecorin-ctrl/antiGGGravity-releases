<Window x:Class="antiGGGravity.Views.General.SetRevisionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Revision on Sheets" Height="700" Width="850"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="55" CornerRadius="12" GlassFrameThickness="0" ResizeBorderThickness="10"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Style="{StaticResource PremiumBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Border Grid.Row="0" Background="{StaticResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource CompanyLogoStyle}"/>
                        <TextBlock Style="{StaticResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Set Revision" Style="{StaticResource PremiumTitleStyle}"/>
                            <TextBlock Text="Set selected revisions on multiple sheets" Style="{StaticResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" 
                            Style="{StaticResource PremiumIconButtonStyle}" Width="30" Height="30"
                            Click="UI_Btn_Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- CONTENT -->
            <Grid Grid.Row="1" Margin="25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT: REVISIONS -->
                <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#F8F9FA" Padding="15,10" CornerRadius="8,8,0,0" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,0,0,1">
                            <TextBlock Text="Select Revisions" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0"/>
                        </Border>

                        <Grid Grid.Row="1" Margin="15,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="UI_Check_AllRevs" VerticalAlignment="Center" Margin="0,0,10,0" Checked="UI_Check_AllRevs_Checked" Unchecked="UI_Check_AllRevs_Unchecked"/>
                            <TextBox x:Name="UI_Txt_SearchRevs" Grid.Column="1" Height="30" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0" Tag="Search Revisions..." TextChanged="UI_Txt_SearchRevs_TextChanged"/>
                        </Grid>

                        <ListBox x:Name="UI_List_Revisions" Grid.Row="2" Margin="15,0,15,15" BorderThickness="1" BorderBrush="#E0E0E0" SelectionMode="Extended">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Label}" FontWeight="SemiBold" FontSize="12"/>
                                            <TextBlock Text="{Binding Date}" FontSize="10" Foreground="#666666"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- RIGHT: SHEETS -->
                <Border Grid.Column="2" Background="White" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#F8F9FA" Padding="15,10" CornerRadius="8,8,0,0" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,0,0,1">
                            <TextBlock Text="Select Sheets" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0"/>
                        </Border>

                        <Grid Grid.Row="1" Margin="15,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Selection Source -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
                                <TextBlock Text="Drawing Selection Source:" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,5"/>
                                <ComboBox x:Name="UI_Combo_Source" Height="32" DisplayMemberPath="Name" SelectionChanged="UI_Combo_Source_SelectionChanged"/>
                            </StackPanel>

                            <CheckBox x:Name="UI_Check_AllSheets" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" Checked="UI_Check_AllSheets_Checked" Unchecked="UI_Check_AllSheets_Unchecked"/>
                            <TextBox x:Name="UI_Txt_SearchSheets" Grid.Row="1" Grid.Column="1" Height="30" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0" Tag="Search Sheets..." TextChanged="UI_Txt_SearchSheets_TextChanged"/>
                        </Grid>

                        <ListBox x:Name="UI_List_Sheets" Grid.Row="2" Margin="15,0,15,15" BorderThickness="1" BorderBrush="#E0E0E0" SelectionMode="Extended">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding DisplayName}" VerticalAlignment="Center" FontSize="12"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>

            <!-- FOOTER -->
            <Border Grid.Row="2" Background="#FAFAFA" Padding="15,10" CornerRadius="0,0,12,12" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock x:Name="UI_Txt_Status" Grid.Column="0" Text="Select revisions and sheets to process." VerticalAlignment="Center" Foreground="#999999" FontSize="11"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Cancel" Width="100" Style="{StaticResource PremiumPrimaryButtonStyle}" Margin="0,0,10,0" Click="UI_Btn_Cancel_Click"/>
                        <Button x:Name="UI_Btn_Apply" Content="Apply Revisions" Width="150" Style="{StaticResource PremiumActionButtonStyle}" Click="UI_Btn_Apply_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
