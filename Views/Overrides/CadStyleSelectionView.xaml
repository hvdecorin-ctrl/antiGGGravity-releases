<Window x:Class="antiGGGravity.Views.Overrides.CadStyleSelectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select CAD Override Style" Height="450" Width="350"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="False" Topmost="True">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="60" ResizeBorderThickness="8" GlassFrameThickness="0" CornerRadius="12"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="StyleButtonStyle" TargetType="Button" BasedOn="{StaticResource PremiumActionButtonStyle}">
                <Setter Property="Height" Value="50"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Padding" Value="15,0"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Background="White" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Background="{StaticResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="20,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="CAD Override" Style="{StaticResource PremiumTitleStyle}" FontSize="18"/>
                        <TextBlock Text="Select a style to apply project-wide" Style="{StaticResource PremiumSubtitleStyle}" FontSize="11"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" 
                            Style="{StaticResource PremiumIconButtonStyle}" Content="✕" Click="Cancel_Click"/>
                </Grid>
            </Border>

            <!-- Styles List -->
            <StackPanel Grid.Row="1" Margin="20,20,20,10">
                <Button Style="{StaticResource StyleButtonStyle}" Click="Style_Click" Tag="1">
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="20" Height="20" Fill="Black" RadiusX="3" RadiusY="3" Margin="0,0,10,0"/>
                        <TextBlock Text="Black / Half / Trans" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource StyleButtonStyle}" Click="Style_Click" Tag="2">
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="20" Height="20" Fill="#FF8000" RadiusX="3" RadiusY="3" Margin="0,0,10,0"/>
                        <TextBlock Text="Orange / Half / Trans" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource StyleButtonStyle}" Click="Style_Click" Tag="3">
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="20" Height="20" Fill="Blue" RadiusX="3" RadiusY="3" Margin="0,0,10,0"/>
                        <TextBlock Text="Blue / Half / Trans" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource StyleButtonStyle}" Click="Style_Click" Tag="4">
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="20" Height="20" Fill="Green" RadiusX="3" RadiusY="3" Margin="0,0,10,0"/>
                        <TextBlock Text="Green / Half / Trans" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource StyleButtonStyle}" Click="Style_Click" Tag="5">
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="20" Height="20" Fill="#8080FF" RadiusX="3" RadiusY="3" Margin="0,0,10,0"/>
                        <TextBlock Text="Purple-Blue / Hidden / Half" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Scope Selection (Implicit in CMD links previously, but here we can make it a footer toggle or separate buttons) -->
            <Border Grid.Row="2" Padding="20,0,20,20" Background="Transparent">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="BtnCurrentView" Content="Apply to View" Click="Scope_Click" Tag="Current" Margin="0,0,5,0" Height="35"/>
                    <Button x:Name="BtnProject" Content="Apply Project" Click="Scope_Click" Tag="Project" Grid.Column="1" Margin="5,0,0,0" Height="35"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
