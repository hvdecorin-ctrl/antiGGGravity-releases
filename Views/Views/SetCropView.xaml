<Window x:Class="antiGGGravity.Views.Views.SetCropView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Crop" Height="320" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="70" CornerRadius="12" GlassFrameThickness="0" ResizeBorderThickness="10"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="IconButtonStyles" TargetType="Button" BasedOn="{StaticResource PremiumPrimaryButtonStyle}">
                <Setter Property="Height" Value="65"/>
                <Setter Property="Margin" Value="0,8"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Style="{StaticResource PremiumBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="15,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource CompanyLogoStyle}"/>
                        <TextBlock Style="{StaticResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Set Crop" Style="{StaticResource PremiumTitleStyle}"/>
                            <TextBlock Text="Draw or reset crop" Style="{StaticResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" 
                            Style="{StaticResource PremiumIconButtonStyle}" Width="24" Height="24"
                            Click="Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid x:Name="BodyContainer" Grid.Row="1" Margin="15">
                <StackPanel VerticalAlignment="Center">
                    <Button x:Name="UI_Button_Draw" Style="{StaticResource IconButtonStyles}" Click="Draw_Click">
                        <StackPanel Orientation="Horizontal" Margin="15,0">
                            <TextBlock Text="✏️" FontSize="24" Margin="0,0,15,0" VerticalAlignment="Center"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Draw Crop Box" FontWeight="Bold" FontSize="14" Foreground="#333333"/>
                                <TextBlock Text="Click and drag to define area" FontSize="10" Foreground="#666666"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>

                    <Button x:Name="UI_Button_Reset" Style="{StaticResource IconButtonStyles}" Click="Reset_Click">
                        <StackPanel Orientation="Horizontal" Margin="15,0">
                            <TextBlock Text="🔄" FontSize="24" Margin="0,0,15,0" VerticalAlignment="Center"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Reset Crop" FontWeight="Bold" FontSize="14" Foreground="#333333"/>
                                <TextBlock Text="Remove all crop boundaries" FontSize="10" Foreground="#666666"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Footer -->
            <Border Grid.Row="2" Padding="15,10" Background="#FAFAFA" CornerRadius="0,0,12,12" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <TextBlock x:Name="UI_Status_Text" Text="Ready" VerticalAlignment="Center" Foreground="#999999"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Close" Click="Cancel_Click" Style="{StaticResource PremiumPrimaryButtonStyle}" Width="80"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
