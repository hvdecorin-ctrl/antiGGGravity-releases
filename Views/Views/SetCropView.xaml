<Window x:Class="antiGGGravity.Views.Views.SetCropView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Crop" Height="380" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="IconButtonStyles" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                <Setter Property="Height" Value="80"/>
                <Setter Property="Margin" Value="0,10"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="{DynamicResource HeaderHeight}"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="{DynamicResource FooterHeight}"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="{DynamicResource HeaderPadding}">
                <TextBlock Style="{StaticResource BrandTitleStyle}"/>
                <Rectangle Width="2" Height="24" Fill="{DynamicResource SeparatorBrush}" Margin="15,0" VerticalAlignment="Center"/>
                <TextBlock Text="Set Crop" Style="{StaticResource BrandSubtitleStyle}"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <Grid x:Name="BodyContainer" Grid.Row="1" Margin="{DynamicResource ContentMargin}">
            <StackPanel VerticalAlignment="Center">
                <Button x:Name="UI_Button_Draw" Style="{StaticResource IconButtonStyles}" Click="Draw_Click">
                    <StackPanel Orientation="Horizontal" Margin="20,0">
                        <TextBlock Text="✏️" FontSize="28" Margin="0,0,20,0" VerticalAlignment="Center"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Draw Crop Box" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="Click and drag to define the crop area" FontSize="11" Foreground="#666666"/>
                        </StackPanel>
                    </StackPanel>
                </Button>

                <Button x:Name="UI_Button_Reset" Style="{StaticResource IconButtonStyles}" Click="Reset_Click">
                    <StackPanel Orientation="Horizontal" Margin="20,0">
                        <TextBlock Text="🔄" FontSize="28" Margin="0,0,20,0" VerticalAlignment="Center"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Reset Crop" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="Remove all crop boundaries from this view" FontSize="11" Foreground="#666666"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="{DynamicResource FooterPadding}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="UI_Status_Text" Text="Ready" Style="{StaticResource StatusLabelStyle}"/>

                <Button Grid.Column="1" Content="Close" Click="Cancel_Click" Width="90" 
                        Style="{StaticResource SecondaryButtonStyle}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
