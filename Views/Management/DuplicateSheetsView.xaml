<Window x:Class="antiGGGravity.Views.Management.DuplicateSheetsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Duplicate Sheets" Height="650" Width="850"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="55" CornerRadius="12" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Style="{StaticResource PremiumBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Border Grid.Row="0" Background="{StaticResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource CompanyLogoStyle}"/>
                        <TextBlock Style="{StaticResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Duplicate Sheets" Style="{StaticResource PremiumTitleStyle}"/>
                            <TextBlock Text="Batch duplicate sheets with or without detailing" Style="{StaticResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" 
                            Style="{StaticResource PremiumIconButtonStyle}" Width="30" Height="30"
                            Click="UI_Btn_Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- CONTENT -->
            <Grid Grid.Row="1" Margin="25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/> <!-- Sheet List -->
                    <ColumnDefinition Width="20"/> <!-- Spacer -->
                    <ColumnDefinition Width="1.2*"/> <!-- Options -->
                </Grid.ColumnDefinitions>

                <!-- LEFT: SHEET LIST -->
                <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="#F8F9FA" Padding="15,10" CornerRadius="8,8,0,0" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,0,0,1">
                            <TextBlock Text="Select Sheets to Duplicate" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0"/>
                        </Border>
                        <TextBox x:Name="UI_Txt_Search" Grid.Row="1" Height="32" Margin="15,10,15,5" Padding="5" VerticalContentAlignment="Center" BorderBrush="#E0E0E0" TextChanged="UI_Txt_Search_TextChanged"/>
                        <ListBox x:Name="UI_List_Sheets" Grid.Row="2" Margin="15,5,15,15" SelectionMode="Extended" BorderThickness="1" BorderBrush="#E0E0E0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" Margin="5,2"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- RIGHT: OPTIONS -->
                <StackPanel Grid.Column="2">
                    <!-- VIEW OPTIONS -->
                    <Border Background="White" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="View Duplication" Style="{StaticResource PremiumSectionLabelStyle}"/>
                            <RadioButton x:Name="UI_Radio_Duplicate" Content="Duplicate" IsChecked="True" Margin="0,5"/>
                            <RadioButton x:Name="UI_Radio_Detailing" Content="With Detailing" Margin="0,5"/>
                            <RadioButton x:Name="UI_Radio_Dependent" Content="As Dependent" Margin="0,5"/>
                        </StackPanel>
                    </Border>

                    <!-- INCLUDE OPTIONS -->
                    <Border Background="White" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="Include Elements" Style="{StaticResource PremiumSectionLabelStyle}"/>
                            <CheckBox x:Name="UI_Check_Legends" Content="Duplicate Legends" IsChecked="True" Margin="0,5"/>
                            <CheckBox x:Name="UI_Check_Schedules" Content="Duplicate Schedules" IsChecked="True" Margin="0,5"/>
                        </StackPanel>
                    </Border>

                    <!-- NAMING OPTIONS -->
                    <Border Background="White" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Naming Rules" Style="{StaticResource PremiumSectionLabelStyle}"/>
                            <TextBlock Text="Sheet Name Suffix" FontSize="11" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBox x:Name="UI_Txt_Suffix" Text=" Copy" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                            <TextBlock Text="Preview: A101 - Plan Copy" Foreground="#999999" Margin="2,8,0,0" FontSize="11" FontStyle="Italic"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- FOOTER -->
            <Border Grid.Row="2" Background="#FAFAFA" Padding="20,15" CornerRadius="0,0,12,12" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock x:Name="UI_Txt_Status" Grid.Column="0" Text="Ready to duplicate sheets" VerticalAlignment="Center" Foreground="#999999"/>

                    <Button x:Name="UI_Btn_Cancel" Grid.Column="1" Content="Cancel" Width="100" Style="{StaticResource PremiumPrimaryButtonStyle}" Margin="0,0,12,0" Click="UI_Btn_Cancel_Click"/>
                    <Button x:Name="UI_Btn_Duplicate" Grid.Column="2" Content="Duplicate Sheets" Width="180" Style="{StaticResource PremiumActionButtonStyle}" Click="UI_Btn_Duplicate_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
