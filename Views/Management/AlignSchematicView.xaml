<Window x:Class="antiGGGravity.Views.Management.AlignSchematicView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Align Schematic Views" Height="650" Width="850"
        MinWidth="700" MinHeight="500"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="70" CornerRadius="12" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Style="{StaticResource PremiumBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Border Grid.Row="0" Background="{StaticResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource CompanyLogoStyle}"/>
                        <TextBlock Style="{StaticResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Align Schematic Views" Style="{StaticResource PremiumTitleStyle}"/>
                            <TextBlock Text="Synchronize plan crop and position across multiple sheets" Style="{StaticResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" 
                            Style="{StaticResource PremiumIconButtonStyle}" Width="30" Height="30"
                            Click="UI_Btn_Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- CONTENT -->
            <Grid Grid.Row="1" Margin="25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT: MAIN SHEET -->
                <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="#F8F9FA" Padding="15,10" CornerRadius="8,8,0,0" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,0,0,1">
                            <TextBlock Text="1. Select Master Sheet" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0"/>
                        </Border>
                        <TextBox x:Name="UI_Txt_Search" Grid.Row="1" Height="32" Margin="15,10,15,5" Padding="5" VerticalContentAlignment="Center" BorderBrush="#E0E0E0" TextChanged="UI_Txt_Search_TextChanged"/>
                        <ListBox x:Name="UI_List_Main" Grid.Row="2" Margin="15,5,15,15" SelectionMode="Single" BorderThickness="1" BorderBrush="#E0E0E0" SelectionChanged="UI_List_Main_SelectionChanged">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Padding="5,2"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- RIGHT: TARGET SHEETS -->
                <Border Grid.Column="2" Background="White" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="#F8F9FA" Padding="15,10" CornerRadius="8,8,0,0" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,0,0,1">
                            <TextBlock Text="2. Select Target Sheets" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0"/>
                        </Border>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="15,10,15,5" HorizontalAlignment="Right">
                            <Button Content="All" Width="50" Height="24" Style="{StaticResource PremiumPrimaryButtonStyle}" Padding="5,0" FontSize="11" Click="UI_Btn_All_Click"/>
                            <Button Content="None" Width="50" Height="24" Style="{StaticResource PremiumPrimaryButtonStyle}" Margin="8,0,0,0" Padding="5,0" FontSize="11" Click="UI_Btn_None_Click"/>
                        </StackPanel>
                        <ListBox x:Name="UI_List_Others" Grid.Row="2" Margin="15,5,15,15" SelectionMode="Extended" BorderThickness="1" BorderBrush="#E0E0E0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" Margin="5,2"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>

            <!-- FOOTER -->
            <Border Grid.Row="2" Background="#FAFAFA" Padding="20,15" CornerRadius="0,0,12,12" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock x:Name="UI_Status_Text" Grid.Column="0" VerticalAlignment="Center" Foreground="#999999"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                       <CheckBox x:Name="UI_Check_Titleblock" Content="Align Titleblock" IsChecked="True" Margin="0,0,15,0"/> 
                       <CheckBox x:Name="UI_Check_Legends" Content="Align Legends" IsChecked="False"/> 
                    </StackPanel>

                    <Button x:Name="UI_Btn_Cancel" Grid.Column="2" Content="Cancel" Width="90" Style="{StaticResource PremiumPrimaryButtonStyle}" Margin="0,0,12,0" Click="UI_Btn_Cancel_Click"/>
                    <Button x:Name="UI_Btn_Align" Grid.Column="3" Content="Align Views" Width="140" Style="{StaticResource PremiumActionButtonStyle}" Click="UI_Btn_Align_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
