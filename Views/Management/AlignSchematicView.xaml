<Window x:Class="antiGGGravity.Views.Management.AlignSchematicView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Align Schematic Views" Height="600" Width="800"
        MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize" Topmost="True"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="{DynamicResource HeaderHeight}"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="{DynamicResource FooterHeight}"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="{DynamicResource HeaderPadding}">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource BrandTitleStyle}"/>
                    <Rectangle Width="2" Height="20" Fill="{DynamicResource SeparatorBrush}" Margin="15,0"/>
                    <TextBlock Text="ALIGN SCHEMATIC" Style="{StaticResource BrandSubtitleStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- CONTENT -->
        <Grid Grid.Row="1" Margin="{DynamicResource ContentMargin}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: MAIN SHEET -->
            <Border Grid.Column="0" Style="{StaticResource SectionBorderStyle}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="{DynamicResource WindowBackgroundBrush}" Padding="15,10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                        <TextBlock Text="1. Select Master Sheet" Style="{StaticResource SectionLabelStyle}" Margin="0"/>
                    </Border>
                    <TextBox x:Name="UI_Txt_Search" Grid.Row="1" Height="32" Margin="15,10,15,5" Padding="5" VerticalContentAlignment="Center" Background="{DynamicResource InputBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" TextChanged="UI_Txt_Search_TextChanged"/>
                    <ListBox x:Name="UI_List_Main" Grid.Row="2" Margin="15,5,15,15" SelectionMode="Single" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" SelectionChanged="UI_List_Main_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" Padding="5,2"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- RIGHT: TARGET SHEETS -->
            <Border Grid.Column="2" Style="{StaticResource SectionBorderStyle}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="{DynamicResource WindowBackgroundBrush}" Padding="15,10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                        <TextBlock Text="2. Select Target Sheets" Style="{StaticResource SectionLabelStyle}" Margin="0"/>
                    </Border>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="15,10,15,5" HorizontalAlignment="Right">
                        <Button Content="All" Width="50" Height="24" Style="{StaticResource SecondaryButtonStyle}" Padding="5,0" FontSize="11" Click="UI_Btn_All_Click"/>
                        <Button Content="None" Width="50" Height="24" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0" Padding="5,0" FontSize="11" Click="UI_Btn_None_Click"/>
                    </StackPanel>
                    <ListBox x:Name="UI_List_Others" Grid.Row="2" Margin="15,5,15,15" SelectionMode="Extended" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" Margin="5,2"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- FOOTER -->
        <Border Grid.Row="2" Background="White" Padding="{DynamicResource FooterPadding}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                   <CheckBox x:Name="UI_Check_Titleblock" Content="Align Titleblock" IsChecked="True" Margin="0,0,15,0"/> 
                   <CheckBox x:Name="UI_Check_Legends" Content="Align Legends" IsChecked="False"/> 
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock x:Name="UI_Status_Text" VerticalAlignment="Center" Margin="0,0,20,0" Style="{StaticResource StatusLabelStyle}"/>
                    <Button x:Name="UI_Btn_Cancel" Content="Cancel" Width="90" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,12,0" Click="UI_Btn_Cancel_Click"/>
                    <Button x:Name="UI_Btn_Align" Content="Align Views" Width="140" Style="{StaticResource PrimaryButtonStyle}" Click="UI_Btn_Align_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
