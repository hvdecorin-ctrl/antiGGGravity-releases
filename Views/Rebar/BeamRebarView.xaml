<Window x:Class="antiGGGravity.Views.Rebar.BeamRebarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Beam Rebar Generator" 
        Height="850" Width="850" 
        MinHeight="700" MinWidth="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" Topmost="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="70" CornerRadius="12" GlassFrameThickness="0" ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border Style="{StaticResource PremiumBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource CompanyLogoStyle}"/>
                        <TextBlock Style="{StaticResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Beam Rebar" Style="{StaticResource PremiumTitleStyle}"/>
                            <TextBlock Text="Generate reinforcement for beams" Style="{StaticResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" 
                            Style="{StaticResource PremiumIconButtonStyle}" Width="30" Height="30"
                            Click="UI_Button_Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- MAIN CONTENT -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Grid Margin="25">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- LEFT COLUMN: TOP & BOTTOM REINFORCEMENT -->
                    <StackPanel Grid.Column="0">
                        <Border Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Top Reinforcement" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,15"/>
                                
                                <!-- T1 Layer -->
                                <Border Background="White" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,10">
                                    <StackPanel>
                                        <CheckBox x:Name="UI_Check_T1" Content="T1 - Top Layer 1 (Outer)" FontSize="13" Foreground="#333333" FontWeight="SemiBold" IsChecked="True" Margin="0,0,0,10" Click="toggle_visibility"/>
                                        <StackPanel x:Name="UI_Group_T1">
                                            <Grid Margin="0,0,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*"/>
                                                    <ColumnDefinition Width="10"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Rebar Type" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                                    <ComboBox x:Name="UI_Combo_T1Type" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Count" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                                    <TextBox x:Name="UI_Text_T1Count" Text="2" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                
                                <!-- T2 Layer -->
                                <Border Background="White" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,15">
                                    <StackPanel>
                                        <CheckBox x:Name="UI_Check_T2" Content="T2 - Top Layer 2 (Inner)" FontSize="13" Foreground="#333333" FontWeight="SemiBold" IsChecked="True" Margin="0,0,0,10" Click="toggle_visibility"/>
                                        <StackPanel x:Name="UI_Group_T2">
                                            <Grid Margin="0,0,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*"/>
                                                    <ColumnDefinition Width="10"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Rebar Type" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                                    <ComboBox x:Name="UI_Combo_T2Type" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Count" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                                    <TextBox x:Name="UI_Text_T2Count" Text="3" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Top Bars Hooks -->
                                <TextBlock Text="Hook Configuration" FontSize="13" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Hook Start" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_TopHookStart" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Hook End" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_TopHookEnd" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15">
                            <StackPanel>
                                <TextBlock Text="Bottom Reinforcement" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,15"/>
                                
                                <!-- B2 Layer -->
                                <Border Background="White" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,10">
                                    <StackPanel>
                                        <CheckBox x:Name="UI_Check_B2" Content="B2 - Bottom Layer 2 (Inner)" FontSize="13" Foreground="#333333" FontWeight="SemiBold" IsChecked="True" Margin="0,0,0,10" Click="toggle_visibility"/>
                                        <StackPanel x:Name="UI_Group_B2">
                                            <Grid Margin="0,0,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*"/>
                                                    <ColumnDefinition Width="10"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Rebar Type" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                                    <ComboBox x:Name="UI_Combo_B2Type" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Count" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                                    <TextBox x:Name="UI_Text_B2Count" Text="3" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                
                                <!-- B1 Layer -->
                                <Border Background="White" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,15">
                                    <StackPanel>
                                        <CheckBox x:Name="UI_Check_B1" Content="B1 - Bottom Layer 1 (Outer)" FontSize="13" Foreground="#333333" FontWeight="SemiBold" IsChecked="True" Margin="0,0,0,10" Click="toggle_visibility"/>
                                        <StackPanel x:Name="UI_Group_B1">
                                            <Grid Margin="0,0,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*"/>
                                                    <ColumnDefinition Width="10"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Rebar Type" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                                    <ComboBox x:Name="UI_Combo_B1Type" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Count" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                                    <TextBox x:Name="UI_Text_B1Count" Text="2" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Bottom Bars Hooks -->
                                <TextBlock Text="Hook Configuration" FontSize="13" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Hook Start" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_BotHookStart" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Hook End" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_BotHookEnd" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- RIGHT COLUMN: Stirrups + Offsets -->
                    <StackPanel Grid.Column="2">
                        <Border Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Stirrups / Links" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,15"/>
                                
                                <TextBlock Text="Rebar Shape" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                <ComboBox x:Name="UI_Combo_TransShape" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0" Margin="0,0,0,15"/>

                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Rebar Type" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_TransType" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Spacing (mm)" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <TextBox x:Name="UI_Text_TransSpacing" Text="200" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Hook Start" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_HookStart" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Hook End" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_HookEnd" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15">
                            <StackPanel>
                                <TextBlock Text="Rule Offsets" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,15"/>
                                <StackPanel>
                                    <TextBlock Text="Stirrup Start Offset (mm)" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                    <TextBox x:Name="UI_Text_TransStartOffset" Text="50" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0" Margin="0,0,0,15"/>
                                </StackPanel>
                                
                                <CheckBox x:Name="UI_Check_RemoveExisting" Content="Clear existing rebar" FontSize="13" Foreground="#666666" IsChecked="True"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <!-- FOOTER -->
            <Border Grid.Row="2" Background="#FAFAFA" Padding="20,15" CornerRadius="0,0,12,12" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Ready to reinforce • Beam Rebar" VerticalAlignment="Center" Foreground="#999999"/>

                    <Button x:Name="UI_Button_Cancel" Grid.Column="1" Content="Cancel" Width="90" Style="{StaticResource PremiumPrimaryButtonStyle}" Margin="0,0,12,0" Click="UI_Button_Cancel_Click"/>
                    <Button x:Name="UI_Button_Generate" Grid.Column="2" Content="Generate" Width="140" Style="{StaticResource PremiumActionButtonStyle}" Click="UI_Button_Generate_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
