<Window x:Class="antiGGGravity.Views.Rebar.WallRebarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wall Rebar Generator" 
        Height="700" Width="800" 
        MinHeight="600" MinWidth="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" Topmost="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="70" CornerRadius="12" GlassFrameThickness="0" ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border Style="{StaticResource PremiumBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource CompanyLogoStyle}"/>
                        <TextBlock Style="{StaticResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Wall Rebar" Style="{StaticResource PremiumTitleStyle}"/>
                            <TextBlock Text="Generate reinforcement for walls" Style="{StaticResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" 
                            Style="{StaticResource PremiumIconButtonStyle}" Width="30" Height="30"
                            Click="UI_Button_Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- MAIN CONTENT (DUAL PANEL) -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Grid Margin="25">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- LEFT COLUMN: Vertical Reinforcement -->
                    <StackPanel Grid.Column="0">
                        <Border Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Vertical Reinforcement" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,15"/>
                                
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Rebar Type" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_VertType" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Spacing (mm)" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <TextBox x:Name="UI_Text_VertSpacing" Text="200" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                </Grid>
                                
                                <TextBlock Text="Rule Offsets" FontSize="13" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Start Offset (mm)" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <TextBox x:Name="UI_Text_VertStartOffset" Text="50" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0" Margin="0,0,0,15"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="End Offset (mm)" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <TextBox x:Name="UI_Text_VertEndOffset" Text="50" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0" Margin="0,0,0,15"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Hooks &amp; Extensions" FontSize="13" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Hook Bottom" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_VertHookStart" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                        <CheckBox x:Name="UI_Check_VertHookStartOut" Content="Bend Outward" FontSize="13" Foreground="#666666" Margin="0,10,0,15"/>
                                        
                                        <CheckBox x:Name="UI_Check_VertBotExt" Content="Bottom Ext (mm)" FontSize="13" Foreground="#666666" IsChecked="False" Margin="0,0,0,8" Click="UI_Check_VertBotExt_Click"/>
                                        <TextBox x:Name="UI_Text_VertBotExt" Text="500" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0" Margin="0,0,0,10" Visibility="Collapsed"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Hook Top" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_VertHookEnd" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                        <CheckBox x:Name="UI_Check_VertHookEndOut" Content="Bend Outward" FontSize="13" Foreground="#666666" Margin="0,10,0,15"/>
                                        
                                        <CheckBox x:Name="UI_Check_VertTopExt" Content="Top Ext (mm)" FontSize="13" Foreground="#666666" IsChecked="False" Margin="0,0,0,8" Click="UI_Check_VertTopExt_Click"/>
                                        <TextBox x:Name="UI_Text_VertTopExt" Text="500" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0" Margin="0,0,0,10" Visibility="Collapsed"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- RIGHT COLUMN: Horizontal Reinforcement -->
                    <StackPanel Grid.Column="2">
                        <Border Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Horizontal Reinforcement" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,15"/>
                                
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Rebar Type" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_HorizType" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Spacing (mm)" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <TextBox x:Name="UI_Text_HorizSpacing" Text="200" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>
                                    </StackPanel>
                                </Grid>
                                
                                <TextBlock Text="Rule Offsets" FontSize="13" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Top Offset (mm)" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <TextBox x:Name="UI_Text_HorizTopOffset" Text="50" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0" Margin="0,0,0,15"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Bottom Offset (mm)" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <TextBox x:Name="UI_Text_HorizBottomOffset" Text="50" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0" Margin="0,0,0,15"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Hooks Configuration" FontSize="13" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Hook Start" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_HorizHookStart" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                        <CheckBox x:Name="UI_Check_HorizHookStartOut" Content="Bend Outward" FontSize="13" Foreground="#666666" Margin="0,10,0,15"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Hook End" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="UI_Combo_HorizHookEnd" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                        <CheckBox x:Name="UI_Check_HorizHookEndOut" Content="Bend Outward" FontSize="13" Foreground="#666666" Margin="0,10,0,15"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Placement Config" FontSize="13" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,8"/>
                                <TextBlock Text="Layer Layout" FontSize="12" Foreground="#666666" Margin="0,0,0,4"/>
                                <ComboBox x:Name="UI_Combo_LayerConfig" Height="32" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0">
                                    <ComboBoxItem Content="Centre" IsSelected="True"/>
                                    <ComboBoxItem Content="Both faces"/>
                                    <ComboBoxItem Content="Internal face"/>
                                    <ComboBoxItem Content="External face"/>
                                </ComboBox>
                                <CheckBox x:Name="UI_Check_RemoveExisting" Content="Clear existing rebar" FontSize="13" Foreground="#666666" IsChecked="True" Margin="0,15,0,5"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <!-- FOOTER -->
            <Border Grid.Row="2" Background="#FAFAFA" Padding="20,15" CornerRadius="0,0,12,12" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Ready to reinforce • Wall Rebar" VerticalAlignment="Center" Foreground="#999999"/>

                    <Button x:Name="UI_Button_Cancel" Grid.Column="1" Content="Cancel" Width="90" Style="{StaticResource PremiumPrimaryButtonStyle}" Margin="0,0,12,0" Click="UI_Button_Cancel_Click"/>
                    <Button x:Name="UI_Button_Generate" Grid.Column="2" Content="Generate" Width="140" Style="{StaticResource PremiumActionButtonStyle}" Click="UI_Button_Generate_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
