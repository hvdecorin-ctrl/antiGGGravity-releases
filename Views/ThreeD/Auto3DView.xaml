<Window x:Class="antiGGGravity.Views.ThreeD.Auto3DView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Auto 3D View" Height="400" Width="700"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize"
        Background="{DynamicResource WindowBackgroundBrush}">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="{DynamicResource HeaderHeight}"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="{DynamicResource FooterHeight}"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="{DynamicResource HeaderPadding}">
                <TextBlock Style="{StaticResource BrandTitleStyle}"/>
                <Rectangle Width="2" Height="24" Fill="{DynamicResource SeparatorBrush}" Margin="15,0" VerticalAlignment="Center"/>
                <TextBlock Text="Auto 3D View" Style="{StaticResource BrandSubtitleStyle}"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="{DynamicResource ContentMargin}">
            <!-- Source View -->
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Source View:" Style="{StaticResource SectionLabelStyle}"/>
                <TextBlock Grid.Column="1" x:Name="lblSourceDescription" Text="View Name (from source)" Foreground="{DynamicResource SubtitleBrush}"/>
            </Grid>

            <!-- 3D View Name -->
            <TextBlock Text="3D View Name:" Style="{StaticResource SectionLabelStyle}" Margin="0,10,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="txtName" Style="{StaticResource FilterTextBoxStyle}"/>
                <TextBlock Grid.Column="1" Text="- W" Foreground="{DynamicResource BrandPrimaryBrush}" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="Bold"/>
            </Grid>

            <!-- 3D View Template -->
            <TextBlock Text="3D View Template:" Style="{StaticResource SectionLabelStyle}" Margin="0,15,0,5"/>
            <ComboBox x:Name="cmbTemplates" Height="30" VerticalContentAlignment="Center"/>
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="{DynamicResource FooterPadding}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="Ready to create • Auto 3D" Style="{StaticResource StatusLabelStyle}"/>
                
                <Button Grid.Column="1" Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" Click="Cancel_Click" Margin="0,0,10,0" Width="100"/>
                <Button Grid.Column="2" Content="Create" Style="{StaticResource PrimaryButtonStyle}" Click="Create_Click" Width="120"/>
            </Grid>
        </Border>
    </Grid>
</Window>
