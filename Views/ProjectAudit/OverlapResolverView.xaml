<Window x:Class="antiGGGravity.Views.ProjectAudit.OverlapResolverView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Resolve Overlaps" Height="420" Width="380"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="False"
        MinHeight="420" MinWidth="380" Topmost="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="OptionCheckBoxStyle" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                <Setter Property="Margin" Value="0,0,0,12"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="55" CornerRadius="12" GlassFrameThickness="0" ResizeBorderThickness="10"/>
    </WindowChrome.WindowChrome>

    <Border BorderBrush="{DynamicResource PremiumBorderBrush}" BorderThickness="1" Background="White" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>   <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" IsHitTestVisible="False">
                        <TextBlock Style="{DynamicResource CompanyLogoStyle}"/>
                        <TextBlock Style="{DynamicResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Resolve Overlaps" Style="{DynamicResource PremiumTitleStyle}"/>
                            <TextBlock Text="Fix overlapping annotations" Style="{DynamicResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" 
                            Style="{DynamicResource PremiumIconButtonStyle}" Width="30" Height="30" Content="✕" 
                            Click="Close_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="25,20,25,10">
                <StackPanel>
                    <!-- Scope Section -->
                    <TextBlock Text="Application Scope" Style="{DynamicResource PremiumSectionLabelStyle}" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" Margin="5,0,0,15">
                        <RadioButton x:Name="RadSelection" Content="Selection" GroupName="Scope" Margin="0,0,20,0"/>
                        <RadioButton x:Name="RadActiveView" Content="Active View" IsChecked="True" GroupName="Scope" Margin="0,0,20,0"/>
                        <RadioButton x:Name="RadEntireProject" Content="Entire Project" GroupName="Scope"/>
                    </StackPanel>

                    <Separator Background="#F0F0F0" Margin="0,0,0,15"/>

                    <!-- Element Types Section -->
                    <TextBlock Text="Element Types" Style="{DynamicResource PremiumSectionLabelStyle}" Margin="0,0,0,10"/>
                    <StackPanel Margin="10,0,0,0">
                        <CheckBox x:Name="ChkTextNotes" Content="Text Notes" IsChecked="True" Style="{StaticResource OptionCheckBoxStyle}"/>
                        <CheckBox x:Name="ChkTags" Content="Tags (All Categories)" IsChecked="True" Style="{StaticResource OptionCheckBoxStyle}"/>
                    </StackPanel>

                    <Separator Background="#F0F0F0" Margin="0,0,0,15"/>

                    <!-- Settings Section -->
                    <TextBlock Text="Settings" Style="{DynamicResource PremiumSectionLabelStyle}" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                        <TextBlock Text="Padding Gap:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox x:Name="ComboPadding" Width="100" Height="30" VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Text="Pinned elements will be temporarily unpinned and moved." 
                               FontStyle="Italic" FontSize="11" Foreground="#999999" Margin="10,15,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" Padding="25,20" Background="#FAFAFA" CornerRadius="0,0,12,12" 
                    BorderBrush="{DynamicResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <TextBlock x:Name="UI_Status" VerticalAlignment="Center" Foreground="#999999" Text="Ready to resolve overlaps."/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Apply" Click="Apply_Click" Style="{DynamicResource PremiumActionButtonStyle}" 
                                Width="90" Height="35" IsDefault="True"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
