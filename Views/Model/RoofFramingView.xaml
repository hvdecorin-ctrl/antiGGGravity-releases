<Window x:Class="antiGGGravity.Views.Model.RoofFramingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Roof Framing (Purlin &amp; Rafter)" 
        Height="800" Width="800"
        MinHeight="600" MinWidth="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" Topmost="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/antiGGGravity;component/Resources/Pre_BrandStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="55" CornerRadius="12" GlassFrameThickness="0" ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>

    <Border Style="{DynamicResource PremiumBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource BrandGradientBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{DynamicResource CompanyLogoStyle}"/>
                        <TextBlock Style="{DynamicResource CompanyDividerStyle}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Purlin &amp; Rafter" Style="{DynamicResource PremiumTitleStyle}"/>
                            <TextBlock Text="Generates framing based on roof slope" Style="{DynamicResource PremiumSubtitleStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" 
                            Style="{DynamicResource PremiumIconButtonStyle}" Width="30" Height="30"
                            Click="UI_Button_Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Grid Margin="25">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- LEFT PANEL: RAFTER -->
                    <Border Grid.Column="0" Grid.Row="0" Background="#FAFAFA" CornerRadius="8" BorderBrush="{DynamicResource PremiumBorderBrush}" BorderThickness="1" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Rafter (Along Slope)" Style="{DynamicResource PremiumSectionLabelStyle}" Margin="0,0,0,15"/>

                            <TextBlock Text="Framing Type" Style="{StaticResource PremiumSectionLabelStyle}" FontSize="12" Margin="0,0,0,6"/>
                            <ComboBox x:Name="UI_Combo_RafterType" Height="32" Margin="0,0,0,15" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>

                            <TextBlock Text="Spacing (mm)" Style="{StaticResource PremiumSectionLabelStyle}" FontSize="12" Margin="0,0,0,6"/>
                            <TextBox x:Name="UI_Text_RafterSpacing" Text="600" Height="32" Margin="0,0,0,15" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>

                            <TextBlock Text="Start Offset (mm)" Style="{StaticResource PremiumSectionLabelStyle}" FontSize="12" Margin="0,0,0,6"/>
                            <TextBox x:Name="UI_Text_RafterOffset" Text="0" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>
                        </StackPanel>
                    </Border>

                    <!-- RIGHT PANEL: PURLIN -->
                    <Border Grid.Column="2" Grid.Row="0" Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Purlin (Perpendicular)" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,15"/>

                            <TextBlock Text="Framing Type" Style="{StaticResource PremiumSectionLabelStyle}" FontSize="12" Margin="0,0,0,6"/>
                            <ComboBox x:Name="UI_Combo_PurlinType" Height="32" Margin="0,0,0,15" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>

                            <TextBlock Text="Spacing (mm)" Style="{StaticResource PremiumSectionLabelStyle}" FontSize="12" Margin="0,0,0,6"/>
                            <TextBox x:Name="UI_Text_PurlinSpacing" Text="400" Height="32" Margin="0,0,0,15" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>

                            <TextBlock Text="Start Offset (mm)" Style="{StaticResource PremiumSectionLabelStyle}" FontSize="12" Margin="0,0,0,6"/>
                            <TextBox x:Name="UI_Text_PurlinOffset" Text="0" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>
                        </StackPanel>
                    </Border>

                    <!-- OPTIONS PANEL -->
                    <Border Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="2" Background="#FAFAFA" CornerRadius="8" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="1" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Placement Options" Style="{StaticResource PremiumSectionLabelStyle}" Margin="0,0,0,15"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Rafter &amp; Purlin Relationship" Style="{StaticResource PremiumSectionLabelStyle}" FontSize="12" Margin="0,0,0,8"/>
                                    <CheckBox x:Name="UI_Check_RafterUnder" Content="Rafter under bottom of purlin" IsChecked="True" Foreground="#666666" Margin="0,4"/>
                                    <CheckBox x:Name="UI_Check_RotateSlope" Content="Rotate cross-section to slope" IsChecked="True" Foreground="#666666" Margin="0,12,0,4"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Generate Model" Style="{StaticResource PremiumSectionLabelStyle}" FontSize="12" Margin="0,0,0,8"/>
                                    <CheckBox x:Name="UI_Check_Purlins" Content="Generate purlins" IsChecked="True" Foreground="#666666" Margin="0,4"/>
                                    <CheckBox x:Name="UI_Check_Rafters" Content="Generate rafters" IsChecked="True" Foreground="#666666" Margin="0,4"/>
                                    
                                    <StackPanel Margin="0,12,0,0">
                                        <CheckBox x:Name="UI_Check_EdgeRafters" Content="Ridge / Hip / Valley (2+ faces)" IsChecked="True" Foreground="#666666" Margin="0,4"/>
                                        <StackPanel Orientation="Horizontal" Margin="22,8,0,0">
                                            <TextBlock Text="Edge Beam Type" Style="{StaticResource PremiumSectionLabelStyle}" FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <ComboBox x:Name="UI_Combo_EdgeType" Width="180" Height="28" Padding="6,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>

                            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                                <CheckBox x:Name="UI_Check_FinishingOffset" Content="Offset for roof finishing layer" IsChecked="False" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="#666666"/>
                                <TextBox x:Name="UI_Text_FinishingOffset" Text="50" Width="60" Height="28" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#E0E0E0"/>
                                <TextBlock Text="mm" Foreground="#666666" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#FAFAFA" Padding="20,15" CornerRadius="0,0,12,12" BorderBrush="{StaticResource PremiumBorderBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" x:Name="UI_Status_Text" Text="Ready to generate • Pick a face first" VerticalAlignment="Center" Foreground="#999999"/>

                    <Button x:Name="UI_Button_Cancel" Grid.Column="1" Content="Cancel" Width="90" Style="{DynamicResource PremiumPrimaryButtonStyle}" Margin="0,0,12,0" Click="UI_Button_Cancel_Click"/>
                    <Button x:Name="UI_Button_Generate" Grid.Column="2" Content="Generate" Width="140" Style="{DynamicResource PremiumActionButtonStyle}" Click="UI_Button_Generate_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
